import React from 'react';
import { ResponsiveContainer } from 'recharts';

const PortfolioPieChart = (props) => {
  return (
    <ResponsiveContainer>
      <div>Inside the responsive container</div>
    </ResponsiveContainer>
  )
}

export default PortfolioPieChart;